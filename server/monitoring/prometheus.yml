global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "libro-server"
    metrics_path: "/api/actuator/prometheus"
    scrape_interval: 5s
    static_configs:
      - targets: ["host.docker.internal:8080"]
        labels:
          application: "libro-server"
